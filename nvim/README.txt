To make neovim read the changes save the entire /nvim/ directory to:
		~/.config
With this command: #use mv instead of cp if you do not want to retain the downloaded directory in its downloaded folder
		#Go to the directory's downloaded folder
		mv nvim ~/.config

If you want to use lua as the theme language instead of vimscript the theme is inside the /lua/ folder.
